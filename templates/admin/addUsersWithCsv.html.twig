{% extends 'base.html.twig' %}


{% block title %}Admin/Ajouter des utilisateurs | {{ parent() }}{% endblock %}
{% block body %}

    {{ form_start(csvForm) }}
    {{ form_widget(csvForm) }}
    <button class="btn btn-primary" type="submit">Valider</button>
    <a class="btn btn-primary" href="{{ path('admin_home') }}">Retour</a>
    {{ form_end(csvForm) }}
{% endblock %}
